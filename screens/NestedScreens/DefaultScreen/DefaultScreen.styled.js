import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  postsContainer: {
    flex: 1,
    flexGrow: 1,
    paddingTop: 32,
    backgroundColor: "#fff",
  },
});

export default styles;
